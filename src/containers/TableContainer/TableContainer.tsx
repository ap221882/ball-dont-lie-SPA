import { StyledTableContainer } from './tableContainer.styles';

type Props = {};

const TableContainer = (props: Props) => {
  return <StyledTableContainer>TableContainer</StyledTableContainer>;
};

export default TableContainer;
