import { StyledSearchBar } from './searchBar.style';

type Props = {};

const SearchBar = (props: Props) => {
  return (
    <StyledSearchBar>
      <input type="text" />
    </StyledSearchBar>
  );
};

export default SearchBar;
